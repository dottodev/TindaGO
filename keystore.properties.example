# Copy to keystore.properties and fill in your release keystore details.
# Do NOT commit keystore.properties (it contains secrets).
storeFile=release.keystore
storePassword=changeit
keyAlias=tindahan
keyPassword=changeit
